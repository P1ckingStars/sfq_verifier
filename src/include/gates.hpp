#ifndef GATES_HPP
#define GATES_HPP

#include "automata.hpp"
using namespace ta;

Automata * WIRE();
Automata * AND_GATE();
Automata * CLK_UNIT();
Automata * NAND_GATE();
Automata * NOR_GATE();
Automata * NOT_GATE();
Automata * OR_GATE();
Automata * XNOR_GATE();
Automata * XOR_GATE();


#endif
